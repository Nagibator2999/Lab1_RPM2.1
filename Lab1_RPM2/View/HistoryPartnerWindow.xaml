<Window x:Class="Lab1_RPM2.View.HistoryPartnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab1_RPM2.View"
        mc:Ignorable="d"
        Title="История реализации продуктов" Height="450" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PartnerTitleText" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid 
            Grid.Row="1"
            ItemsSource="{Binding ProductPartner}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            CanUserAddRows="False"
            RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул продукции" Binding="{Binding Article_product}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity_products}" Width="Auto"/>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding Date_sales, StringFormat=d}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
